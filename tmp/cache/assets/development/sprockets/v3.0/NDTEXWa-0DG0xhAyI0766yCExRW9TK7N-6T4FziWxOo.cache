I"Õ(function() {
  jQuery(function() {
    var regexp, time;
    $('form').on('click', '.remove_fields', function(event) {
      $(this).prev('input[type=hidden]').val('1');
      $(this).closest('tr').hide();
      return event.preventDefault();
    });
    return $('add_fields').click(time = new Date().getTime(), regexp = new RegExp($(this).data('id'), 'g'), $(this).before($(this).data('fields').replace(regexp, time)), event.preventDefault());
  });

}).call(this);
:ET